{
  "name": "kafka-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/kafka-admin/src",
  "tags": ["type:lib", "scope:shared"],
  "targets": {
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsx --tsconfig tsconfig.json libs/kafka-admin/src/cli.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
